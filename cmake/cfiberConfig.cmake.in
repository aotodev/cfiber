@PACKAGE_INIT@

include(CMakeFindDependencyMacro)
if(@CFIBER_REACTOR@)
    find_dependency(Threads)
endif()

include("${CMAKE_CURRENT_LIST_DIR}/cfiberTargets.cmake")

check_required_components(cfiber)
